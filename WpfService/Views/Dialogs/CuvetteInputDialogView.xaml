<UserControl x:Class="WpfService.Views.Dialogs.CuvetteInputDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             Height="200" Width="450">

    <Grid Margin="8" VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0"
                   Visibility="{Binding IsRange, Converter={dxmvvm:BooleanToVisibilityConverter}}"
                   Margin="8"
                   VerticalAlignment="Center"
                   Text="Range"
                   FontSize="16"/>

        <Grid Grid.Row="0" Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <dxe:SpinEdit Grid.Column="0"
                          Visibility="{Binding IsRange, Converter={dxmvvm:BooleanToVisibilityConverter}}"
                          IsReadOnly="{Binding IsReadOnly}"
                          Margin="8"
                          FontFamily="Roboto"
                          FontSize="18"
                          MinValue="200"  MaxValue="{Binding RangeTo}" Increment="10"
                          IsFloatValue="False"
                          EditValue="{Binding RangeFrom, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                          VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
            <TextBlock Grid.Column="1"
                       Visibility="{Binding IsRange, Converter={dxmvvm:BooleanToVisibilityConverter}}"
                       Margin="8"
                       TextAlignment="Center"
                       VerticalAlignment="Center"
                       Text="~"
                       FontSize="16"/>
            <dxe:SpinEdit Grid.Column="2"
                          Visibility="{Binding IsRange, Converter={dxmvvm:BooleanToVisibilityConverter}}"
                          IsReadOnly="{Binding IsReadOnly}"
                          Margin="8"
                          FontFamily="Roboto"
                          FontSize="18"
                          MinValue="{Binding RangeFrom}" MaxValue="800" Increment="10"
                          IsFloatValue="False"
                          EditValue="{Binding RangeTo, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                          VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
        </Grid>

        <TextBlock Grid.Row="1" Grid.Column="0"
                   Margin="8"
                   VerticalAlignment="Center"
                   Text="WaveLength"
                   FontSize="16"/>

        <dxe:SpinEdit Grid.Row="1" Grid.Column="1"
                      IsReadOnly="{Binding IsReadOnly}"
                      Margin="8"
                      FontFamily="Roboto"
                      FontSize="18"
                      MinValue="{Binding RangeFrom}" MaxValue="{Binding RangeTo}" Increment="10"
                      Mask="d"
                      EditValue="{Binding Wavelength, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>

    </Grid>
</UserControl>

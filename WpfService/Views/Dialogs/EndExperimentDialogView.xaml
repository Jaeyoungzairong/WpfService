<UserControl x:Class="WpfService.Views.Dialogs.EndExperimentDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             Height="200" Width="450">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0"
                   Margin="8"
                   VerticalAlignment="Center"
                   Text="Title"
                   FontSize="16"/>
        <dxe:TextEdit Grid.Row="0" Grid.Column="1" 
                      Margin="8"
                      FontFamily="Roboto"
                      FontSize="18"
                      MaxLength="30"
                      NullText="Default Name"
                      ShowNullTextIfFocused ="True"
                      HorizontalContentAlignment="Center"
                      Text="{Binding TitleText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
    </Grid>
</UserControl>
